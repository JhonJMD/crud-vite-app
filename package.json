{
  "name": "crud-vite-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server" : "json-server server/db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.0",
    "vite": "^5.2.0"
  }
}
